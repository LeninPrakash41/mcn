<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- Allow JIT compilation (needed by some JS engines) -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>

  <!-- Allow unsigned executable memory (Electron requirement) -->
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>

  <!-- Disable library validation to allow loading helper processes -->
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>

  <!-- Network client access (to reach the local Python server) -->
  <key>com.apple.security.network.client</key>
  <true/>

  <!-- Network server access (Python server listens on localhost) -->
  <key>com.apple.security.network.server</key>
  <true/>

  <!-- Read-write access to files selected by the user -->
  <key>com.apple.security.files.user-selected.read-write</key>
  <true/>
</dict>
</plist>
